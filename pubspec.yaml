name: securecar_api
description: An new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bcrypt: ^1.1.3 # Criptografia bcrypt.
  dart_frog: ^0.3.8 
  dart_frog_auth: ^0.1.0
  dotenv: ^4.1.0 # Variáveis de ambiente.
  mysql1: ^0.20.0 # Banco de dados MariaDB.

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.24.4
  very_good_analysis: ^5.0.0+1
